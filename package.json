{
  "name": "atrilha-frontend",
  "private": true,
  "version": "0.0.1",
  "description": "Build assets de frontend (Tailwind v4) — orquestrado por frontend-maven-plugin",
  "scripts": {
    "build:css": "tailwindcss -i src/main/frontend/css/app.css -o target/classes/static/css/app.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^4.0.0",
    "@tailwindcss/cli": "^4.0.0"
  }
}
